// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface RouteContext {
  // Add services and dependencies here as needed
  // Example: db: Database, logger: Logger, etc.
}
